<section id="wallet-section" class="card">
    <div class="section-header">
        <div>
            <h2 class="play-mode-title">Wallet</h2>
            <h2 class="info-mode-title" style="display: none;">Load or Import a Wallet</h2>
        </div>
        <img
                src="/assets/images/icon-question.png"
                alt="mode switch"
                class="mode-switch-icon"
        />
    </div>

    <div class="play-mode slide-toggle open">
        <!-- If we have no wallet loaded (e.g. no .env, no phrase yet), show phrase input -->
        <div id="noWalletSection" style="<%= walletDetected ? 'display: none;' : '' %>">
            <p>No .env wallet found. Load your wallet from a recovery phrase.</p>
            <div id="recoveryPhraseSection">
            <textarea
                    id="recoveryPhraseInput"
                    rows="3"
                    style="width:100%;"
                    placeholder="Enter your 12/24 word phrase here"
            ></textarea>
            </div>
            <button id="importWalletBtn" class="btn btn-primary">Load Wallet from Recovery Phrase</button>
        </div>

        <!-- If we already have a wallet (from .env or loaded phrase), show address -->
        <div id="walletLoadedSection" style="<%= walletDetected ? '' : 'display: none;' %>">
            <p>Server wallet loaded at address:
                <span id="currentWalletAddress"><%= walletAddress %></span>
            </p>
        </div>

        <div id="wallet-status"></div>
    </div>

    <div class="info-mode slide-toggle">
        <div class="info-mode-title" style="display: none;">
            <h2>Load or Import a Wallet</h2>
        </div>
        <p>
            You can define <code>MY_WALLET_ADDRESS</code> and <code>MY_WALLET_PRIVATE_KEY</code>
            in your <code>.env</code>, or just import a wallet from a recovery phrase.
        </p>
        <p>This is of course intended to be used with test wallets, so you can play around with the Overledger API.</p>
    </div>
</section>
